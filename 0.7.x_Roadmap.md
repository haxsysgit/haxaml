# Haxaml `0.7.x` Roadmap

`0.7.x` is the setup and workflow accommodation line.

`0.6.x` made Haxaml coherent as a governed workflow and MCP runtime. `0.7.0` already shipped the setup foundation. The rest of `0.7.x` is about moving Haxaml from "installed correctly" to "active inside real agent workflows" using workflow-native integration points that official vendor docs now expose.

Haxaml is the governance spine for agent work, not an AI memory product. Memory, archive retrieval, and diary state remain supporting systems for governed execution, not the headline. The `0.7.x` line therefore focuses on getting real agents into the existing governed lifecycle:

`about -> guidance -> prebuild -> context_pack -> verify -> record -> expect_sync`

## Research Method

The setup and workflow-capability claims below were re-verified against current official vendor docs on May 13, 2026.

Roadmap policy for this line:

- Automate only stable, documented, file-backed, config-backed, agent-backed, or hook-backed integration points.
- Keep UI-only or weakly documented workflow behavior as guidance-only.
- Prefer tool-specific workflow accommodation over inventing parallel Haxaml workflow runtimes.
- Keep pre-`1.0` workflow help guidance-first. Hard blocking mainly belongs in `0.8.0`.

## `0.7.x` Product Position

`0.7.x` has one job: make Haxaml usable where agents already live.

This means five concrete shifts:

- setup is already shipped in `0.7.0`; the remaining work is workflow accommodation depth
- workflow should move from reactive tool-calling toward proactive lifecycle steering
- materials gathering must become more visible in governed flow, especially owner-provided dependencies and `materials_needed`
- context-budget planning is product usability work, not just token-efficiency work
- diary and handoff state must synthesize decisions and failures, not just accumulate raw Acts history

Semantic retrieval remains strategically important, but it does not belong in `0.7.x` as a promised default feature. It stays on the pre-`1.0` path as a research-first track in `0.9.0`.

## `0.7.0` Delivered Foundation

`0.7.0` is already the delivered setup foundation for the line.

Delivered direction:

- `haxaml setup` as the main onboarding command
- `haxaml setup doctor` for install audit, drift detection, and manual follow-up reporting
- `haxaml setup print` for inspect-before-write output
- one shared target registry for setup behavior and docs
- one canonical Haxaml operating contract rendered into tool-specific setup outputs
- explicit project-scope versus user-scope handling

`0.7.0` solved "how do I install Haxaml into this environment?" The remaining `0.7.x` releases solve "how does that environment keep Haxaml active during real work?"

## `0.7.1` - Workflow Accommodation Foundation

Goal: extend setup from static onboarding assets into workflow-native outputs.

Planned work:

- add roadmap support for workflow-native setup outputs, not just instructions, skills, agents, and MCP
- extend `haxaml setup` with a `workflow` asset class so setup can install or print workflow-native assets alongside `frame`, `instructions`, `skills`, `agents`, and `mcp`
- add a shell-safe workflow guard CLI integration point for hooks, custom agents, and remote/background environments
- keep the workflow guard explicit as a machine-friendly entrypoint with exit codes and `text`/`json` output
- keep hooks and remote agents on CLI-safe helpers rather than routing them through a new MCP tool

Primary targets for `0.7.1`:

- Claude Code hooks
- GitHub Copilot custom agents
- OpenCode skills and agents
- Junie portable skills
- Codex config-backed workflow assets

## `0.7.2` - Provider Workflow Depth

Goal: deepen target-specific workflow accommodation where vendor docs are now strong enough.

Planned work:

- Claude Code: hook matchers, steering, and selective gating patterns
- GitHub Copilot: `.github/agents`, `.github/skills`, and repository-vs-user MCP precedence
- Cursor: background-agent setup assets and `.cursor/environment.json` guidance where the workflow integration point is file-backed and documented
- OpenCode: skill-first and agent-first workflow integration with per-agent MCP/tool enablement
- Junie: portable skill-first workflow integration and subagent-aware handoff
- keep unsupported or UI-only workflow integration points guidance-only

## `0.7.3` - Workflow Doctor And Drift Audit

Goal: make workflow accommodation inspectable, not just installable.

Planned work:

- keep `haxaml setup doctor` as the single advisory audit entrypoint for setup-managed and workflow-managed files
- make workflow doctor output provider-aware so managed files are described as hook scripts, hook config, background environments, custom agents, adapter files, local runners, CI runners, and workflow MCP/config entrypoints instead of generic manifest entries
- enrich doctor JSON with optional workflow metadata while keeping the top-level `installed / missing / drifted / manual_actions / message` report shape stable
- keep scope limited to the workflow-capable targets already shipped in `0.7.2`
- keep `workflow check` as the stricter readiness path and do not add a failing doctor mode in `0.7.3`

## `0.7.4` - Interactive Setup And Skill Overhaul

Goal: turn setup into a confident onboarding UX instead of a thin file generator.

Planned work:

- make `haxaml setup` interactive by default in a real TTY while keeping `--format json` and `--non-interactive` machine-friendly
- treat target detection as advisory, not authoritative: strong tool-specific files can preselect targets, but shared weak signals like `AGENTS.md` must not silently resolve multiple providers
- replace the thin generated `SKILL.md` checklist with valid YAML-frontmatter skills that actually encode lifecycle triggers, success criteria, fallback behavior, escalation rules, and provider-aware examples
- add narrow safe merges for known MCP config shapes so Haxaml can update only `mcpServers.haxaml` or `[mcp_servers.haxaml]` without clobbering unrelated user config
- replace count-only setup output with exact paths plus short previews so users can see what will change before Haxaml writes anything

## `0.7.5` - Materials, Context Discipline, And Handoff Quality

Goal: make governed flow outputs easier to act on after setup itself becomes trustworthy.

Planned work:

- make `materials_needed` and owner-provided dependencies more prominent in generated guidance and workflow outputs
- improve task-aware, risk-aware, and model-aware context budget planning
- tie token discipline to workflow usability and reliability, not only efficiency metrics
- add decision synthesis so the next agent can see why earlier work happened
- add better failure summaries so unresolved work is understandable without log archaeology
- keep this framed as workflow handoff polish, not as a memory-product expansion

## Setup Foundation Matrix

`0.7.0` remains the baseline setup release. Not every setup target becomes a workflow-depth target in `0.7.1+`, but all rows below describe the still-relevant setup foundation.

| Target | Project-scope setup integration points | User-scope setup integration points | `0.7.0` baseline |
| --- | --- | --- | --- |
| Claude Code | `CLAUDE.md`, `.claude/skills/`, `.claude/agents/`, `.mcp.json` | `~/.claude/CLAUDE.md`, `~/.claude/skills/`, `~/.claude/agents/`, `~/.claude.json` | Full instructions, skills, agents, and MCP support. |
| Codex CLI | `AGENTS.md`, `.agents/skills/`, `.codex/config.toml` | `~/.codex/AGENTS.md`, `~/.agents/skills/`, `~/.codex/config.toml` | Full instructions, skills, and config-backed support. |
| Cursor | `.cursor/rules/*.mdc`, `.cursor/mcp.json` | user rules in settings, `~/.cursor/mcp.json` | Project rules and project/user MCP. User rules remain guidance-only. |
| Windsurf | `.windsurf/rules/*.md`, `AGENTS.md`, `.windsurf/skills/` | `~/.codeium/windsurf/memories/global_rules.md`, `~/.codeium/windsurf/skills/`, `~/.codeium/windsurf/mcp_config.json` | Project rules and skills, user rules/skills/MCP, no repo-local MCP promise. |
| GitHub Copilot | `.github/copilot-instructions.md`, `.github/skills/` | `~/.copilot/copilot-instructions.md`, `~/.copilot/skills/`, `~/.copilot/mcp-config.json` | Instructions and skills, plus user MCP and project guidance where repo MCP was not yet the headline. |
| Gemini CLI | `GEMINI.md`, `.gemini/settings.json`, `.gemini/skills/` | `~/.gemini/settings.json`, `~/.gemini/skills/` | Full project and user setup support. |
| Continue | `.continue/rules/` plus printed config merge guidance | `~/.continue/config.yaml`, `~/.continue/rules/` | Rules are file-backed; project config merges remain manual. |
| Cline | `.clinerules/`, `.cline/skills/` | `~/Documents/Cline/Rules` or `~/Cline/Rules`, `~/.cline/skills/`, `~/.cline/data/settings/cline_mcp_settings.json` | Project rules and skills, plus user rules/skills/MCP. |
| OpenCode | `AGENTS.md`, `.opencode/skills/`, `opencode.json[c]` | `~/.config/opencode/AGENTS.md`, `~/.config/opencode/skills/`, `~/.config/opencode/opencode.json[c]` | Full instructions and skills, with config-backed MCP support where schema and path are documented. |
| Junie | `.junie/AGENTS.md`, `.junie/skills/`, `.junie/agents/`, `.junie/mcp/mcp.json` | `~/.junie/skills/`, `~/.junie/agents/`, `~/.junie/mcp/mcp.json` | Full guidelines, skills, subagents, and MCP support on Junie-native paths. |

## Workflow Accommodation Matrix

This matrix is the main roadmap addition for `0.7.1+`. It captures workflow-native integration points that go beyond setup-only installation.

| Target | Current doc-backed workflow integration points | `0.7.x` accommodation intent | `0.7.x` support policy |
| --- | --- | --- | --- |
| Claude Code | JSON-configured hooks in project/user settings, matcher-driven steering, hook scripts by project path, skills, agents | Generate workflow assets for hooks and route hook decisions into the existing Haxaml lifecycle | Strong automation target. Hooks are stable enough for steering and selective gating. |
| Codex CLI | `AGENTS.md`, `.agents/skills/`, `.codex/config.toml`, documented hooks, `codex exec` non-interactive mode, GitHub Action | Add config-backed workflow assets and guard-compatible CLI helpers for local, CI, and remote use | Keep the workflow story centered on skills, config, non-interactive mode, and GitHub Action integration points. Do not make hook-enforced governance the baseline cross-runtime promise for `0.7.x`. |
| Cursor | rules, MCP, background agents, committed `.cursor/environment.json` machine setup | Treat background-agent environment setup as a workflow adapter file distinct from rules/MCP | Automate only documented file-backed assets. Keep UI-only flows guidance-only. |
| GitHub Copilot | `.github/agents`, `.github/skills`, project MCP in `.mcp.json` or `.github/mcp.json`, repository/user precedence, custom-agent MCP/tool scoping | Generate repo-native custom-agent and skill assets that steer work into Haxaml without inventing a parallel runtime | Strong automation target where file-backed and documented. Respect repository-over-user precedence and custom-agent tool restrictions. |
| OpenCode | `.opencode/skills/`, `.opencode/agents/`, `opencode.json[c]`, per-agent permissions, per-agent MCP/tool enablement | Make skill-first and agent-first workflow accommodation a first-class output, especially for scoped MCP/tool access | Strong automation target. OpenCode's agent and per-agent MCP model is directly compatible with governed workflow accommodation. |
| Junie | `.junie/AGENTS.md`, `.junie/skills/`, `.junie/mcp/mcp.json`, custom subagents, cross-agent skill import suggestions | Treat portable skills and Junie-specific subagents as first-class accommodation integration points | Strong automation target for file-backed CLI assets. Keep IDE-only project settings guidance-only. |

## Important Planned Public Integration Points

These public integration points should be explicit in the roadmap so implementation intent is not ambiguous:

- `haxaml setup --only workflow`
- workflow as a first-class setup asset category alongside `frame`, `instructions`, `skills`, `agents`, and `mcp`
- a shell-safe workflow guard CLI integration point for hooks, custom agents, and remote/background environments
- machine-readable workflow guard output in `json` as well as human-readable `text`
- exit-code failure for non-interactive workflow checks
- no new workflow MCP tool in `0.7.x`

`0.7.x` workflow accommodation should route tool-specific workflow assets into the existing governed lifecycle. Hooks, CI jobs, custom agents, and remote agents need CLI-safe helpers that call into Haxaml's established flow, not a second parallel workflow API.

## Out Of Scope

The `0.7.x` line stays disciplined about what workflow accommodation is not:

- no dashboard expansion into a workflow control plane
- no secret manager
- no UI automation for vendor settings dialogs
- no undocumented private config-path guessing
- no new memory-product positioning
- no default semantic-retrieval rollout

## Acceptance Checks

Roadmap consistency:

- `0.7.x_Roadmap.md` and `v1.0_Roadmap.md` must both state that `0.7.0` setup is already shipped
- `v1.0_Roadmap.md` must link to `0.7.x_Roadmap.md`, not `0.7.x.md`
- release numbering must stay monotonic across `0.7.0`, `0.7.1` through `0.7.5`, `0.8.0`, `0.9.0`, and `1.0.0`

Source alignment:

- every workflow-native automation claim must remain tied to a current official source
- unsupported or UI-only workflow integration points must stay guidance-only
- tool-specific workflow integration should prefer vendor-supported files, hooks, agents, configs, and CLI integration points over speculative abstractions

Product-language checks:

- Haxaml should be described primarily as workflow governance, not as memory tooling
- materials gathering and context budgeting should be treated as workflow usability features
- diary improvements should emphasize decision synthesis and handoff quality
- semantic retrieval must remain labeled as research-first on the pre-`1.0` path

## Sources

### Claude Code

- [Settings](https://code.claude.com/docs/en/settings)
- [Hooks reference](https://code.claude.com/docs/en/hooks)
- [Connect Claude Code to tools via MCP](https://code.claude.com/docs/en/mcp)
- [How Claude remembers your project](https://code.claude.com/docs/en/memory)
- [Extend Claude with skills](https://code.claude.com/docs/en/skills)
- [Create custom subagents](https://code.claude.com/docs/en/subagents)

### Codex

- [Custom instructions with AGENTS.md](https://developers.openai.com/codex/guides/agents-md)
- [Agent Skills](https://developers.openai.com/codex/skills)
- [Configuration Reference](https://developers.openai.com/codex/config-reference)
- [Hooks](https://developers.openai.com/codex/hooks)
- [Non-interactive Mode](https://developers.openai.com/codex/noninteractive)
- [Codex GitHub Action](https://developers.openai.com/codex/github-action)

### Cursor

- [Rules](https://docs.cursor.com/en/context/rules)
- [Model Context Protocol](https://docs.cursor.com/advanced/model-context-protocol)
- [Background Agents](https://docs.cursor.com/en/background-agents)

### Windsurf

- [Cascade Memories](https://docs.windsurf.com/windsurf/cascade/memories)
- [Cascade Skills](https://docs.windsurf.com/windsurf/cascade/skills)
- [AGENTS.md](https://docs.windsurf.com/windsurf/cascade/agents-md)
- [Cascade MCP Integration](https://docs.windsurf.com/windsurf/cascade/mcp)

### GitHub Copilot

- [Add custom instructions](https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/add-custom-instructions)
- [GitHub Copilot CLI configuration directory](https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-config-dir-reference)
- [Adding agent skills for GitHub Copilot](https://docs.github.com/en/copilot/how-tos/use-copilot-agents/coding-agent/create-skills)
- [Using GitHub Copilot CLI custom agents](https://docs.github.com/en/copilot/how-tos/copilot-cli/use-copilot-cli-agents/overview)
- [Custom agents configuration](https://docs.github.com/en/copilot/reference/custom-agents-configuration)

### Gemini CLI

- [Configuration reference](https://github.com/google-gemini/gemini-cli/blob/main/docs/reference/configuration.md)
- [MCP server tool docs](https://github.com/google-gemini/gemini-cli/blob/main/docs/tools/mcp-server.md)
- [Settings](https://github.com/google-gemini/gemini-cli/blob/main/docs/cli/settings.md)
- [CLI reference](https://github.com/google-gemini/gemini-cli/blob/main/docs/cli/cli-reference.md)

### Continue

- [Rules](https://docs.continue.dev/customize/rules)
- [MCP deep dive](https://docs.continue.dev/customize/deep-dives/mcp)
- [Config reference](https://docs.continue.dev/reference/config)

### Cline

- [Cline Rules](https://docs.cline.bot/customization/cline-rules)
- [Skills](https://docs.cline.bot/customization/skills)
- [CLI configuration](https://docs.cline.bot/cline-cli/configuration)
- [Adding and configuring MCP servers](https://docs.cline.bot/mcp/adding-and-configuring-servers)

### OpenCode

- [Config](https://dev.opencode.ai/docs/config/)
- [Agent Skills](https://dev.opencode.ai/docs/skills/)
- [Agents](https://dev.opencode.ai/docs/agents/)
- [MCP servers](https://dev.opencode.ai/docs/mcp-servers/)

### Junie

- [Guidelines and memory](https://junie.jetbrains.com/docs/guidelines-and-memory.html)
- [Agent skills](https://junie.jetbrains.com/docs/agent-skills.html)
- [Custom subagents](https://junie.jetbrains.com/docs/junie-cli-subagents.html)
- [Add and configure MCP servers](https://junie.jetbrains.com/docs/junie-cli-mcp-configuration.html)
- [CLI reference](https://junie.jetbrains.com/docs/parameters.html)
