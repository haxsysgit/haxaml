# 0.4 Review Gate (Pending Diff)

Date: 2026-04-28
Scope: current uncommitted changes in `ADVANCED.md`, `haxaml/adoption.py`, `haxaml/init_templates.py`, `tests/test_cli.py`.
Verification run: `uv run pytest -q` -> `172 passed in 14.24s`.

## Severity-Ranked Findings

### Medium

1. Policy assertions are substring-based and can pass malformed policy text.
- Evidence: [tests/test_cli.py](/home/hax/haxaml/tests/test_cli.py#L37), [tests/test_cli.py](/home/hax/haxaml/tests/test_cli.py#L38), [tests/test_cli.py](/home/hax/haxaml/tests/test_cli.py#L108), [tests/test_cli.py](/home/hax/haxaml/tests/test_cli.py#L109)
- Risk: a partial string match can still pass even if wording drifts, list values are truncated, or policy meaning changes unintentionally.
- Required outcome: assert exact expected policy strings are present in generated `rules.yaml` (and ideally exactly once each).

### Low

1. Commit-message policy text is duplicated in multiple generators and docs.
- Evidence: [haxaml/adoption.py](/home/hax/haxaml/haxaml/adoption.py#L307), [haxaml/adoption.py](/home/hax/haxaml/haxaml/adoption.py#L320), [haxaml/init_templates.py](/home/hax/haxaml/haxaml/init_templates.py#L86), [ADVANCED.md](/home/hax/haxaml/ADVANCED.md#L72)
- Risk: copy drift over time (wording mismatch between init/adopt/docs).
- Recommendation: centralize policy strings in one shared constant in a follow-up refactor.

## Required Fixes (Must-Fix Before 0.4.1 Coding)

- [x] Replace substring assertions with exact-string assertions for commit policy checks in `tests/test_cli.py`.
- [x] Re-run `uv run pytest -q` after that test hardening change.

## Go / No-Go Decision for 0.4.1 Implementation

Status: `GO`

Reason: required-fixes checklist is closed and regression tests are green (`173 passed in 10.92s`).

## Review Closure Criteria

Review is considered closed when all required-fix checkboxes above are complete and test results are green.
